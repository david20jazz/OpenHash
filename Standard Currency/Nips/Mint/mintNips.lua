--[[-- 
Create 10 billions of nips of which have unique IP addresses.
(1) Select a block of IPv6 address.
(2) Determine nip units: 1, 5, 10, 20, 30  ... 1,000,000,000.
(3) Map each nip to each IP address. 
--]] --

-- Select a few number(300) of IP addresses from Subnet ID. Each of which represent a nation.
function selectIPBlock()
end

-- Determine nip units from 1 to 1 billion.
function nipUnits()
end

-- Allocate nips to each unit.
function mintNips()
end