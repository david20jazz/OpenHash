--[[
    Define the procedure of a transaction between the machine and a Jisel.
    ]]

--     First call txn() from Onnuri module, and return it to doTxn(.
function callTxnFromOnnuri()
end 

function doTxn(txn)
    local transaction = txn

end
