--[[-- 
1) Called by Onnuri webpage.
2) Initialize Onnuri server(xmpp).
3) By clicking/touching the entry button on the Onnuri webpate, visiters are forwarded to Onnuri server. 
4) The visiters send their GPS data to Onnuri server. 
5) 
--]]--
-- Initialize Onnuri server.
initOnnuri()

-- Return GPS info. 
getPermission()

-- Forward visiters to address server
forwardToAddressServer(player)
