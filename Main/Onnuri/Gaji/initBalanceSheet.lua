-- Initialize BS object and assign it to the user data f a player
-- Retrieve BS form, IS  and player object from DB and pass it as parameter..

function initBalanceSheet(bs,isParam player)
    local balanceSheet = bs
    local incomestatement = isParam

    -- Initialize balanceSheet using already written IS and player object.


    return balanceSheet
end
