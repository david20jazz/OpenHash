--[[-- 
Let's assume all players are human, and connect to Onnuri server using smartphone, pc, tablet, and smartwatch.
1) Initialize Onnuri server(xmpp).
2) Initialize Onnuri webpage(http).
3) By clicking/touching the entry button on the Onnuri webpate, visiters are forwarded to Onnuri server. 
4) The visiters send their GPS data to Onnuri server. 
5) Use machine learning to establish the hierarchical structure of the network. 
    - One method is to classify the population of a nation into two groups based on geographical distribution, and divide each group again into two groups, and so on. 
--]] --





function initOnnuri()

end

